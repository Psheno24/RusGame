# Домен из .env → SITE_DOMAIN=game.ваш-домен.ru (без https://)
# Переменная SITE_DOMAIN должна быть в окружении контейнера caddy (см. docker-compose.yml).
{$SITE_DOMAIN} {
	encode gzip
	reverse_proxy app:3001
}
