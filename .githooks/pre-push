#!/bin/sh
# Не даёт отправить в remote код, который не собирается (как в Docker).
set -e
cd "$(git rev-parse --show-toplevel)"
echo "pre-push: сборка..."
npm run build
echo "pre-push: тесты API..."
npm test -w @russia-game/api
echo "pre-push: OK"
